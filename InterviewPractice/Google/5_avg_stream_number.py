# Given a stream of integers, a value k and a value w, consider the integers 
# in the window w and chop off greater k and smaller k elements from the window w. 
# From the remaining elements, compute the average.

